# 1. Tell Hibernate to STOP generating buggy Enum constraints in tests!
spring.jpa.properties.hibernate.mapping.enum_check_constraint=false

# 2. Tell Spring to always start with a fresh database for tests
spring.jpa.hibernate.ddl-auto=create-drop